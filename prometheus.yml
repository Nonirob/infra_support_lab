global:
  # Prometheus will scrape metrics every 15 seconds.
  scrape_interval: 15s

scrape_configs:
  # Collect Linux host metrics from node-exporter.
  - job_name: "node-exporter"
    static_configs:
      - targets:
          - "node-exporter:9100"